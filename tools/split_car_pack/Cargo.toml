[package]
name = "split-car-pack"
version = "0.1.0"
edition.workspace = true
repository.workspace = true
license.workspace = true
description = "Split the bundled passenger-car-pack glb into per-car glbs with normalized origins and named wheel nodes"

[dependencies]
glam = { workspace = true }
gltf = { workspace = true, features = ["names", "utils"] }
gltf-json = { workspace = true, features = ["names"] }
image = { workspace = true, features = ["png"] }
serde_json = { workspace = true }

[lints]
workspace = true
