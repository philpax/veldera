[package]
name = "bake-earth-topography"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "One-shot bake of ETOPO 2022 elevation data into the runtime asset used by the cloud weather model"

[dependencies]
png = "0.18.1"
reqwest = { version = "0.13", default-features = false, features = ["blocking"] }
tiff = "0.11.3"

[lints]
workspace = true
